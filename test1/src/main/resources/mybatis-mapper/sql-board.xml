<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.test1.mapper.BoardMapper">
	<select id="boardList" parameterType="hashmap" resultType="com.example.test1.model.Board">
		SELECT 
			B.*, 
			NVL(CMT_CNT, 0) AS CCOUNT, 
			DECODE( TO_CHAR(CDATETIME, 'YYYY-MM-DD'), TO_CHAR(SYSDATE, 'YYYY-MM-DD'), TO_CHAR(CDATETIME,'HH24:MI:SS'), TO_CHAR(CDATETIME, 'YYYY-MM-DD') ) AS CDATE
		FROM TBL_BOARD B
		LEFT JOIN (
		    SELECT BOARDNO, COUNT(*) AS CMT_CNT
		    FROM TBL_COMMENT
		    GROUP BY BOARDNO
		)T ON B.BOARDNO = T.BOARDNO
		WHERE 1=1 <!-- 1=1을 하면 언제나 참이 됨 -->
		<if test="kind != '' and kind != null">
			AND KIND = #{kind}
		</if>
		
		<!--검색 옵션-->
		<if test="srchOption == 'all'">
			AND (
				TITLE LIKE '%' || #{keyword} || '%' OR 
				B.USERID LIKE '%' || #{keyword} || '%'
			)
		</if>
		<if test="srchOption == 'title'">
			AND TITLE LIKE '%' || #{keyword} || '%'
		</if>
		<if test="srchOption == 'id'">
			AND B.USERID LIKE '%' || #{keyword} || '%'
		</if>
		
		<!-- order는 jsp에 있는 변수 order -->
		<if test="order == 'date'">
			ORDER BY CDATETIME DESC
		</if>
		<if test="order == 'num'">
			ORDER BY B.BOARDNO ASC
		</if>
		<if test="order == 'title'">
			ORDER BY TITLE ASC
		</if>
		<if test="order == 'cnt'">
			ORDER BY CNT DESC
		</if>
		OFFSET #{page} ROWS FETCH NEXT #{pageSize} ROWS ONLY
	</select>	
	
	<select id="boardCnt" parameterType="hashmap" resultType="int">
		SELECT 
			COUNT(*) 
		FROM TBL_BOARD B
		WHERE 1=1 <!-- 1=1을 하면 언제나 참이 됨 -->
		<if test="kind != '' and kind != null">
			AND KIND = #{kind}
		</if>
		
		<!--검색 옵션-->
		<if test="srchOption == 'all'">
			AND (
				TITLE LIKE '%' || #{keyword} || '%' OR 
				B.USERID LIKE '%' || #{keyword} || '%'
			)
		</if>
		<if test="srchOption == 'title'">
			AND TITLE LIKE '%' || #{keyword} || '%'
		</if>
		<if test="srchOption == 'id'">
			AND B.USERID LIKE '%' || #{keyword} || '%'
		</if>
		
	</select>
		
	<delete id="deleteBoard" parameterType="hashmap">
		DELETE
		FROM TBL_BOARD
		WHERE BOARDNO = #{boardNo}
	</delete>
	
	<delete id="deleteBoardList" parameterType="hashmap">
		DELETE
		FROM TBL_BOARD
		WHERE BOARDNO IN
		<foreach collection="list" item="item" index="index" open="(" close=")" separator=","> 
		<!--sql에서의 반복은 foreach 사용, 반복하고자 하는 것은 collection에 넣음, 꺼낼 때의 이름은 item으로 설정, index는 -->
		<!--open과 close가 없으면 직업 괄호를 쳐주어야 함, separator는 구분자, 값이 하나 나올때마다 뒤에 구분자를 붙여줌 -->
			#{item}
			
		</foreach>
	</delete>
	
	<insert id="insertBoard" 
			parameterType="hashmap" 
			useGeneratedKeys="true" 
			keyColumn="BOARDNO" 
			keyProperty="boardNo">
	<!-- useGeneratedKeys=>key값 생성해서 리턴, keyColumn=>리턴받고자하는 컬럼 이름, keyProperty=>리턴받을 때 쓸 이름 -->
	<!-- 리턴한 mapper 안의 map에 boardNo를 담아서 리턴 -->
	<!-- select 처럼 resultType 사용 불가 -->
		INSERT INTO TBL_BOARD
		VALUES(B_SEQ.NEXTVAL, #{titleInput}, '', #{userIdInput}, 0, 0, 2, SYSDATE, SYSDATE, #{contentsInput})
	</insert>
	
	<select id="selectBoard" parameterType="hashmap" resultType="com.example.test1.model.Board">
		SELECT * 
		FROM TBL_BOARD
		WHERE BOARDNO = #{boardNo}
	</select>
	
	<update id="updateBoard" parameterType="hashmap">
		UPDATE TBL_BOARD
		SET 
			TITLE = #{titleInput},
			CONTENTS = #{contentsInput},
			USERID = #{userIdInput}
		WHERE BOARDNO = #{boardNo}
	</update>
	
	<select id="selectCommentList" parameterType="hashmap" resultType="com.example.test1.model.Comment">
		SELECT C.*, NVL(C.P_COMMENTNO, 0) AS PCMT, U.NICKNAME
		FROM TBL_COMMENT C
		INNER JOIN TBL_USER U ON C.USERID = U.USERID
		WHERE C.BOARDNO = #{boardNo}
		ORDER BY C.CDATETIME ASC
	</select>
	
	<insert id="insertComment" parameterType="hashmap">
		INSERT INTO TBL_COMMENT
		VALUES(COMMENT_SEQ.NEXTVAL, #{boardNo}, #{contents}, #{id}, 0, NULL, SYSDATE, SYSDATE)
	</insert>
	
	<update id="updateCnt" parameterType="hashmap"> 
		UPDATE TBL_BOARD
		SET 
			CNT = CNT+1
		WHERE BOARDNO = #{boardNo}
		<!-- 이 방식은 새로고침이나 댓글 달 때마다 조회수가 계속 상승 -->
	</update>
	
	<delete id="deleteComment" parameterType="hashmap">
		DELETE
		FROM TBL_COMMENT
		WHERE COMMENTNO = #{commentNo}
	</delete>
	
	<insert id="insertBoardImg" parameterType="hashmap">
		INSERT INTO TBL_BOARD_FILE
		VALUES(FILE_SEQ.NEXTVAL, #{boardNo}, #{path}, #{fileName}, #{orgName}, #{size}, #{ext})
	</insert>
	
	<select id="selectFileList" parameterType="hashmap" resultType="com.example.test1.model.Board">
		SELECT *
		FROM TBL_BOARD_FILE
		WHERE BOARDNO = #{boardNo}
	</select>
	
</mapper>




