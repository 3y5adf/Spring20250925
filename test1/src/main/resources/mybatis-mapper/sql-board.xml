<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.test1.mapper.BoardMapper">
	<select id="boardList" parameterType="hashmap" resultType="com.example.test1.model.Board">
		SELECT B.*, TO_CHAR(CDATETIME, 'YYYY-MM-DD') AS CDATE
		FROM TBL_BOARD B
		WHERE 1=1
		<!-- 1=1을 하면 언제나 참이 됨 -->
		<if test="kind != '' and kind != null">
			AND KIND = #{kind}
		</if>
		<!--<if test="boardNo != '' and boardNo != null">
			AND BOARDNO = #{boardNo}
		</if>-->
		
		<!-- order는 jsp와 연결? -->
		<if test="order == 'num'">
			ORDER BY BOARDNO ASC
		</if>
		<if test="order == 'title'">
			ORDER BY TITLE ASC
		</if>
		<if test="order == 'cnt'">
			ORDER BY CNT DESC
		</if>
		
	</select>	
		
	<delete id="deleteBoard" parameterType="hashmap">
		DELETE
		FROM TBL_BOARD
		WHERE BOARDNO = #{boardNo}
	</delete>
	
	<insert id="insertBoard" parameterType="hashmap">
		INSERT INTO TBL_BOARD
		VALUES(B_SEQ.NEXTVAL, #{titleInput}, #{contentsInput}, #{userIdInput}, 0, 0, 2, SYSDATE, SYSDATE)
	</insert>
	
	<select id="selectBoard" parameterType="hashmap" resultType="com.example.test1.model.Board">
		SELECT * 
		FROM TBL_BOARD
		WHERE BOARDNO = #{boardNo}
	</select>
	
	<update id="updateBoard" parameterType="hashmap">
		UPDATE TBL_BOARD
		SET 
			TITLE = #{titleInput},
			CONTENTS = #{contentsInput},
			USERID = #{userIdInput}
		WHERE BOARDNO = #{boardNo}
	</update>
	
</mapper>




